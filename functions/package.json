{
  "name": "fortimind-functions",
  "description": "Funciones backend seguras para FortiMind (DeepSeek y Gemini)",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "dotenv": "^17.0.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2",
    "fortimind": "file:.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
